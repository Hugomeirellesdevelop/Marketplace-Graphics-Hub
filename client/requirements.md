## Packages
recharts | For dashboard analytics charts (orders over time)
framer-motion | For smooth page transitions and card animations
date-fns | For date formatting in tables and lists
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge Tailwind classes without conflicts

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Oswald'", "sans-serif"],
  body: ["'Inter'", "sans-serif"],
}
